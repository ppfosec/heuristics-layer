# Private heuristics repository

This folder is the authoritative private source of record. ChatGPT or Claude owns the file operations. The practitioner approves substantive judgment through conversation and does not edit or reconcile these files manually.

When asked to normalize and publish, read `NORMALIZE_AND_PUBLISH.md`, `REPOSITORY_STATE.md`, the current library, and pending intake before making any publication recommendation.

Everything in this folder is private by default. Never copy it into the public Heuristics Layer repository or another destination without explicit authorization.
