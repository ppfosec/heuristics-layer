# ADR 0002: One knowledge object with typed facets

Status: accepted

Use one `heuristic` object in v0.1. It contains a principle plus optional triggers, probes, exceptions, compensating factors, escalation conditions, and examples. Separate object types would create premature taxonomy. A future type is justified only if retrieval or lifecycle rules materially diverge.
